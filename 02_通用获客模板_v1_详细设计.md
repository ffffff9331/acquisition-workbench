# 通用获客模板 Base Template v1：详细设计

> 范围：仅实现通用获客模板，不加入行业插件、客户专属规则、平台 API、CRM、剪辑、设计或自动群发。
> 目标：让一个用户从“这周想获客”走到“知道下周继续什么”，不需要先理解复杂运营术语。

## 0. 产品总原则

产品围绕一个中心对象工作：**获客项目**。

```text
一个客户
  可以有多个获客项目
    一个项目可以有多条内容
      一条内容可以有多个渠道版本
        一个渠道版本可以带来多条线索
```

用户每次打开某个客户，不应面对空白工作台，而是看到一句明确的下一步：

```text
还没建项目：创建本周获客项目
还没选题：选择第一条内容
内容待制作：完成内容包
内容待发布：标记为已发布
有咨询未归因：补充线索来源
项目到期：开始复盘
```

## 1. 页面一：客户列表

### 用户要完成什么

创建、进入、复制一个客户工作空间。客户只是“项目和内容属于谁”的容器，不在这里做获客策略。

### 页面结构

```text
顶部：搜索客户 | 新建客户

客户列表：
客户名称
业务描述
进行中的项目数
下一步待办
最近一次活动时间
```

### 新建客户表单

**必填：**

```text
客户名称
业务描述（卖什么/提供什么）
```

**可选：**

```text
负责人或出镜人
所在地/服务范围
联系方式
已有的产品资料、案例链接、图片或文档
```

### 核心操作

```text
新建客户：创建一个空客户工作空间
进入客户：进入该客户的项目列表与当前待办
复制客户：复制客户基本资料和已确认的通用内容结构；不复制线索数据
归档客户：隐藏不用的客户，不删除历史项目
```

### 系统规则

```text
客户可为空白，但不能有内容、线索、复盘脱离客户存在。
新建客户后，默认引导用户“创建第一个获客项目”。
```

### 首版验收

用户在 1 分钟内能新建客户，并在列表中看见下一步待办。

---

## 2. 页面二：项目设定

### 用户要完成什么

用不超过 3 分钟说清楚：这一次为什么做内容、想吸引谁、希望他做什么。

### 页面结构

采用单页分步表单，不要弹出多个窗口。

```text
步骤 1：目标
步骤 2：目标场景
步骤 3：内容方向
步骤 4：确认项目
```

### 步骤 1：目标

**字段：**

```text
希望对方完成的关键行为：下拉 + 自定义
目标数量：数字
截止时间：日期
```

系统默认提供不带行业色彩的行为类型：

```text
咨询
预约
申请资料
报名/登记
添加联系方式
购买前沟通
自定义
```

**校验：**

```text
不可只填写“曝光”“涨粉”“提升品牌”。
若用户输入模糊目标，提示把它转成可观察行为。
```

### 步骤 2：目标场景

用户不需要填写复杂画像，填一段结构化描述即可：

```text
我想吸引谁：____________
他现在遇到什么问题/处于什么情境：____________
我可以提供什么解决方案、帮助或价值：____________
```

系统把这三项组合为“目标场景摘要”，显示在项目顶部，之后生成内容时自动带入。

### 步骤 3：内容方向

Base Template 固定提供四个内容意图，用户最多选两个：

```text
教育：解释问题、给方法、避免错误
证明：展示案例、过程、能力、结果
比较：说明选择标准、方案差异、风险或成本差异
行动：围绕资料、咨询、预约、活动推动下一步
```

每个方向点开后，系统给 3 个通用选题提问框，而不是直接给结论：

```text
教育：目标人群最常误解什么？
证明：我们有什么过程、案例或细节可展示？
比较：他们做选择时最纠结什么？
行动：有什么低门槛的下一步值得尝试？
```

用户可：

```text
从系统建议中选择
手工写一个选题
保存为项目中的内容待办
```

### 步骤 4：确认项目

确认后创建项目卡：

```text
项目名：系统根据目标自动命名，用户可修改
项目状态：进行中
内容目标：默认 1-3 条，用户可改
当前阶段：待生产第一条内容
```

### 首版验收

用户能在不写任何提示词的情况下创建一个有明确目标、目标场景和 1-3 个待生产选题的项目。

---

## 3. 页面三：内容流水线

### 用户要完成什么

把一个选题变成可直接交付给拍摄、设计或发布人员的内容包，并完成渠道版本和追踪设置。

### 页面布局

```text
左侧：项目内内容列表与状态
中间：当前内容编辑区
右侧：项目目标、目标场景、CTA、发布状态
```

内容卡状态固定为：

```text
待生产 -> 待制作 -> 待发布 -> 已发布 -> 已复盘
```

### 3.1 选题

**字段：**

```text
选题名称
这条内容想解决的问题
选择的内容方向（教育/证明/比较/行动）
```

**系统动作：**

将项目目标与目标场景带入，生成 3 个候选标题方向。用户可选择、编辑或重生成。

### 3.2 内容结构

所有内容采用同一个最小结构，界面不强制用户拆成许多小格：

```text
开头：用什么问题、冲突、结果或观点吸引注意
主体：想讲清楚的核心信息；可插入案例、数据或过程
结尾：CTA，明确用户下一步动作
```

**生成按钮：**

```text
生成第一版
换一个角度
精简
更具体
改成口语表达
```

这些是受限操作，不提供开放式“问 AI 任何问题”的聊天框。

### 3.3 可选证据素材

用户可附加：

```text
案例说明
数据或结果
产品/服务细节
过程图片或视频
客户评价
资质、方法、清单
```

系统只在用户提供这些素材后，才允许生成“基于案例/数据”的表达；没有资料时不能编造事实。

### 3.4 制作要求

用户选择内容形态：

```text
短视频
图文
长文
口播提纲
```

系统输出制作清单：

```text
所需画面或图文页数
需要准备的素材
镜头/段落顺序
封面或首图需要表达什么
发布前检查项
```

### 3.5 CTA 与追踪码

这两个区域必须分开显示。

**CTA：**

```text
希望用户做什么：私信关键词 / 点击链接 / 填表 / 预约 / 领取资料 / 自定义
用户将获得什么：一句清晰说明
```

**追踪码：**

```text
系统为每个内容生成内容编号
系统为每一个渠道版本生成来源编号
可附加短链、二维码、表单链接或关键词
```

首版即使用户没有任何外部工具，也应允许用“内容编号 + 手工来源选择”运行。

### 3.6 渠道发布包

用户勾选渠道后，系统为每个渠道生成一个独立版本：

```text
标题
正文或脚本
封面/首图文字
发布文案
CTA
来源编号
```

用户可单独编辑每个版本。所有版本都来源于同一个内容项，但不能互相覆盖。

### 3.7 发布确认

发布不做自动化，用户只需：

```text
复制内容
手动发布到平台
回到系统点击“标记已发布”
填写实际发布时间和发布链接（链接可选）
```

### 首版验收

用户在 15 分钟内能把一个选题变成至少一个完整、可复制、带 CTA 和追踪编号的渠道发布包。

---

## 4. 页面四：线索追踪

### 用户要完成什么

不管理完整销售过程，只回答“这条咨询是不是来自某条内容，以及是否有价值”。

### 页面结构

```text
顶部：本周期总线索 | 已关联来源比例 | 有效线索数
左侧：内容/渠道筛选
中间：线索列表
右侧或抽屉：新建/编辑线索
```

### 新建线索表单

**必填：**

```text
来源内容：选择内容项
来源渠道版本：选择渠道版本
质量：高 / 中 / 低
```

**建议填写：**

```text
联系方式或匿名标识
需求描述
来源时间
当前状态：待跟进 / 已转化 / 已放弃
备注
```

### 质量的通用解释

首版不给用户伪造精确评分模型，只提供可编辑定义：

```text
高：符合目标人群，需求明确，愿意采取下一步行动
中：符合目标人群，有兴趣，但需求或时间不明确
低：仅浏览、泛咨询、与目标不匹配或无法继续联系
```

### 系统规则

```text
线索可先由内容编号识别；识别不到时允许人工指定来源。
没有来源内容的记录不可计入内容效果排行。
“已转化”只记录为线索状态，不进入订单、金额和销售流程。
```

### 首版验收

用户在 30 秒内能把一条咨询关联到内容和渠道，并标记质量。

---

## 5. 页面五：项目复盘

### 用户要完成什么

只做一次决策：下一个周期该继续、优化还是停止什么。

### 数据输入

首版允许手工录入，字段保持最少：

```text
每个渠道版本的播放/阅读量（可选）
每个内容带来的线索数（自动汇总）
每个内容带来的高/中质量线索数（自动汇总）
本项目可选成本（如有）
```

### 页面输出

**内容排行：**

```text
内容名称
渠道版本
曝光数据（如有）
线索数
有效线索数
有效线索率
```

**三类比较：**

```text
内容方向比较：教育 / 证明 / 比较 / 行动
渠道比较：不同渠道的线索和质量
CTA 比较：不同下一步动作带来的线索质量
```

**最终决策卡：**

```text
继续：什么内容方向/渠道/CTA 值得多做
优化：什么内容有兴趣但行动不足，需要改哪里
停止：什么内容持续没有有效机会，不再投入
```

系统生成建议时必须把“依据数据”显示在建议旁边；用户可编辑并确认最终结论。

### 项目结束

用户点击“完成复盘”后：

```text
项目状态变为已完成
系统创建一个“复用建议”：下一个项目可以复制哪些内容结构和 CTA
历史数据保留，但不能被后续项目覆盖
```

### 首版验收

用户一眼能回答：哪条内容最有效、为什么、下一周打算做什么。

---

## 6. 五个页面之间的状态流转

```text
新建客户
  -> 创建项目（进行中）
     -> 创建内容项（待生产）
        -> 完成内容包（待制作）
           -> 生成渠道版本（待发布）
              -> 标记已发布（等待线索）
                 -> 录入线索（可持续进行）
                    -> 发起复盘（已复盘）
                       -> 完成项目（已完成）
```

产品应在每一次状态转换时给出单一下一步按钮。例如：

```text
待生产：开始写第一条内容
待制作：查看制作清单
待发布：复制发布包
已发布：记录第一条咨询
项目到期：开始复盘
```

## 7. 首版不做，避免把模板做散

```text
开放式 AI 对话
自动剪辑、自动做图
平台自动发布与账号管理
私信自动回复
复杂表单搭建器
销售 CRM、订单、回款
任务/审批/团队项目管理
插件、行业规则与客户规则编辑器
```

Base Template 首版只证明一件事：用户能通过它持续跑通“目标 -> 内容 -> 线索 -> 复盘”的完整闭环。
