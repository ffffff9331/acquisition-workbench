# 行业规则包重构 v1

## 一、重构结论

行业包不是标题库、文案库或演示草稿集合。行业包的职责是把某个行业中可复用的判断方法提供给通用获客流程，具体内容必须结合当前商家资料与真实来源动态形成。

```text
客户实际情况
+ 当前联网证据
+ 行业规则包
+ 渠道基础能力
= 本次具体选题、标题、正文和执行方案
```

缺少客户实际情况或真实证据时，规则包只能提示补充，不能生成看似完整但无法证明的内容。

## 二、三类能力边界

### 渠道基础能力

负责平台或执行场景本身的通用工作流：选题、制作、素材、检查、人工发布、咨询承接和结果记录。抖音、小红书、微信、B站、线下活动和转介绍合作均属于渠道能力。

渠道能力不预设行业，也不自动登录、自动发布、自动私信或自动群发。

### 行业规则包

负责某个行业的：

- 产品、服务和场景词典
- 搜索扩词方向
- 高意向需求信号
- 来源优先、降权和拒绝规则
- 机会筛选维度与权重
- 尺寸、价格、性能、案例等证据要求
- 内容制作约束
- 禁用表达与业务边界
- 各渠道的行业适配规则

行业规则包不保存固定标题、固定正文、固定活动或固定转介绍对象。

### 打法包

打法包是未来在渠道能力和行业规则之上增加的阶段性执行方案，例如明确的业务目标、周期、资源条件、渠道组合、内容节奏、承接动作和复盘指标。

打法包不能绕过联网证据，也不能把历史案例当作当前客户可直接复制的结果承诺。

## 三、数据契约

通用接口定义在 `src/industry-rules.ts`，核心对象为 `IndustryRulePack`：

```text
id / legacyIds / name / industry / version / description
taxonomy
searchDirections
demandSignals
sourceRules
opportunityRules
evidenceRules
productionRules
boundaries
prohibitedPhrases
channelRules
```

选题机会保存 `industryPackId`，内容任务从机会继承同一个 `industryPackId`。只有任务记录的 ID 与当前启用规则包一致时，工作台才会应用该规则，避免切换行业后错误约束历史任务。

## 四、运行流程

### 1. 启用规则

用户启用卫浴规则包后，工作台只记录规则包 ID。不会启用任何渠道，也不会创建选题、草稿、活动或合作关系。

### 2. 动态搜索

行业搜索方向把当前商家填写的主推产品、目标客户、服务地区与行业扩词合并成查询。生成的查询会随商家输入变化，不保存固定选题。

### 3. 形成机会

`acquisition_opportunities` 请求同时接收：

- 当前获客任务简报
- 当前保存的真实来源
- 当前行业规则
- 已验证的历史执行结果

模型必须基于当前简报与来源形成机会。行业词典只能帮助识别，不得被写成没有证据的事实。

### 4. 本地筛选

工作台使用确定性规则检查行业匹配、决策信号、来源证据、商家证明、承接动作和表达风险。分数表示当前资料的制作准备度，不表示平台流量、咨询数量或成交概率。

### 5. 内容生产

`content_draft` 同时接收内容简报、真实来源、渠道表达规则、行业规则和已采用的本地学习规则。缺少行业要求的证明时，模型必须写入素材计划或事实核对项，不得编造。

### 6. 审核与修订

本地审核追加行业禁用表达和证据要求检查。`content_review` 与 `content_revision` 接收同一份行业规则；无法由现有来源与素材解决的问题必须保留为待处理项。

## 五、旧数据迁移

旧字段 `installedContentPackages` 会迁移为 `installedIndustryPacks`。

旧卫浴包 ID：

```text
bathroom-industry-content-v1
```

迁移为新规则包 ID：

```text
bathroom-industry-rules-v2
```

旧机会和旧内容任务没有 `industryPackId` 时保持为空，因此不会被新规则自动重算或重写。渠道对象中的 `contentPackageId` 暂时作为历史追溯字段保留，新机会进入渠道时写入对应行业规则包 ID。

## 六、验证标准

一份行业规则包只有同时满足以下条件才能作为可售能力：

1. 启用后不产生任何固定标题、正文或演示数据。
2. 搜索查询随商家输入和所选方向变化。
3. AI 请求能够完整携带规则包数据。
4. 机会卡能显示确定性的行业准备度与失败原因。
5. 内容生成、审核和修订使用同一个规则包版本。
6. 缺少证据时明确提示，不补造价格、性能、案例或效果。
7. 切换或停用规则后不错误修改历史任务。
8. 桌面和窄屏界面无文本遮挡、横向溢出或不可操作控件。
9. Windows 安装包可以构建；真实 Windows 10 / 11 安装仍需在目标机器人工验证。

## 七、卫浴首包范围

`bathroom-industry-rules-v2` 覆盖马桶、智能马桶、浴室柜、花洒、淋浴房、地漏和龙头，以及到店选购、上门量尺、安装、更换、局部改造和售后等服务。

首版重点验证安装适配、空间动线、排水防潮、价格交付和旧房更换五类动态研究方向，并对尺寸、价格、改造前后、性能效果和客户案例设置证据规则。

这份规则包是行业判断样本，不代表软件已经拥有所有卫浴经营知识。后续迭代应以真实搜索记录、门店咨询、交付资料和执行结果为依据。
