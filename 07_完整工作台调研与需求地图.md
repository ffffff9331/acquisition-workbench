# 完整获客工作台：调研与需求地图

## 结论先说

“完整工作台”不是一组页面，也不是一个能安装功能的 Windows 软件。

它至少要同时具备六个系统：

```text
1. 用户工作台：客户每天实际使用的地方。
2. 功能目录：客户浏览、预览和申请能力的地方。
3. 方案装配器：根据客户选择组装专属工作台的地方。
4. 运营管理台：你管理客户、授权、内容包和更新的地方。
5. 内容与 AI 引擎：让内容、话术和复盘真正有用的地方。
6. 安全、更新与服务：让 Windows 软件能够长期交付的地方。
```

缺任意一层，产品都可以演示，但很难稳定销售、升级和规模化交付。

## 一、应该研究的对象与得到的启发

### 1. 插件平台

VS Code、Obsidian、Atlassian Forge 的共同点：

```text
扩展必须有唯一标识、版本、描述和清晰声明。
扩展不是只放一个页面；它可以贡献导航、命令、设置、视图和数据能力。
扩展要声明兼容性、依赖关系和需要的权限。
发布、升级、下架和归属是平台能力，不是手工文件管理。
```

对获客工作台的启发：

```text
每个功能包必须有 manifest。
每个功能包必须有版本、依赖、预览、兼容版本与升级说明。
每个功能包要声明：增加什么页面、字段、工作流、内容库、指标和 AI 规则。
```

### 2. 工作流与模板平台

n8n 的共同点：

```text
模板库不是文档列表，而是可搜索、可预览、可导入的工作流起点。
工作流和真实运行数据需要分开管理。
模板可由组织提供，客户只看到被允许使用的目录。
```

对获客工作台的启发：

```text
功能目录必须能预览“装上以后会多什么”。
客户的真实业务数据不能和预览数据混在一起。
方案包要支持一键组装、更新与回滚。
```

### 3. 企业解决方案分发

Power Platform 的共同点：

```text
一个解决方案可以包含多个组件、数据表、流程和页面。
解决方案有发布者、版本与依赖。
面向客户环境发布的解决方案应是受管理的交付物。
升级和删除都需要处理依赖与数据迁移。
```

对获客工作台的启发：

```text
你卖给客户的单位应该是“方案包”，不是散装功能。
行业方案包可以包含多个功能、行业资料和专属配置。
客户不能修改、拆解或伪造由你发布的方案包。
升级方案包时必须保留客户已填写的数据。
```

### 4. Windows 桌面软件

Electron 的共同点：

```text
Windows 软件需要安装、代码签名、发布和自动更新能力。
更新不只是重新发一个 exe；需要版本、更新说明与发布地址。
```

对获客工作台的启发：

```text
Core 和方案包都要分别有版本。
以后客户不应靠人工重新安装每一次更新。
Windows 安装包和方案包都必须可验证来源。
```

## 二、完整工作台的六个系统

## 系统 A：客户每天使用的工作台

这是客户看见的产品。它必须遵循一个原则：

```text
客户只看见已开通、与自己有关的功能。
```

### 必需能力

| 能力 | 客户看到什么 | 为什么必要 |
|---|---|---|
| 我的业务 | 门店、品牌、业务资料 | 多业务数据不串台 |
| 今日待办 | 当前该做什么、还缺什么 | 客户不会被功能淹没 |
| 已开通功能 | 只显示已启用的菜单 | 工作台保持简单 |
| 数据录入 | 内容、咨询、素材、结果等 | 形成真实运营数据 |
| 搜索与筛选 | 找到过去内容、客户需求、素材 | 工作台用久后仍能用 |
| 数据备份 | 本机备份、恢复、导出 | 防止客户数据丢失 |
| 使用帮助 | 功能说明、空状态、示例操作 | 降低培训成本 |

### 不应该出现在客户界面里的内容

```text
插件 ID
依赖关系
manifest
方案包版本冲突
签名验证
部署术语
行业配置字段
```

## 系统 B：功能中心与预览

功能中心不是设置页，而是客户理解“还能解决什么问题”的地方。

### 每个可开通方案的详情页至少需要

```text
名称与适合对象
解决的问题
开通后会多出哪些功能
真实界面预览
需要客户准备什么资料
最终能记录什么结果
当前状态：已开通 / 可申请 / 试用中 / 已到期
申请开通入口
```

### 必须分开的两种数据

```text
预览数据：用于展示功能，不写入客户真实工作台。
业务数据：客户自己创建和积累的真实数据。
```

## 系统 C：方案装配器

这是你真正的护城河。它决定一个客户开通方案后，工作台如何自动变出来。

### 方案装配器需要做什么

```text
读取方案包
检查 Core 版本是否兼容
检查方案包之间是否有依赖或冲突
确认客户有没有授权
启用所需功能
加入导航、页面、字段和工作流
导入行业内容与模板
创建默认的空白业务结构
记录本次安装与版本
失败时可以回滚
```

### 方案包的最小组成

```text
唯一 ID
显示名称
发布者
版本号
适用的 Core 版本
依赖的功能包
可见的导航与页面
字段和数据结构
行业内容、模板和话术
AI 提示词与使用边界
默认工作流
预览素材
升级规则与数据迁移规则
授权范围
签名
```

## 系统 D：你的运营管理台

客户使用的是工作台；你需要一个完全不同的管理面。

### 你必须能管理

| 管理对象 | 需要的能力 |
|---|---|
| 客户 | 客户资料、授权方案、到期时间、设备数量、服务记录 |
| 方案 | 草稿、测试、发布、下架、版本、价格、适用行业 |
| 功能 | 依赖、兼容版本、是否可单独开通 |
| 行业内容 | 场景、选题、脚本、CTA、话术、咨询分类、复盘规则 |
| 发布 | 指定哪些客户可见、可安装、可升级 |
| 版本 | 当前版本、待升级版本、升级记录、回滚记录 |
| 服务 | 客户问题、部署记录、培训记录、续费或加购机会 |

### 管理台和客户版要分开

```text
客户版：简单、只做自己的业务。
你的管理台：复杂、用来创建方案、授权、部署和维护。
```

第一阶段不必先做完整在线管理台。可以先做一个“部署人员模式”，由你在安装现场使用；但数据模型必须从第一天就支持未来管理台。

## 系统 E：内容、知识与 AI 引擎

获客工作台真正有价值的地方，不是把表格电子化，而是帮客户少想、少写、少漏步骤。

### AI 要读取什么

```text
客户业务资料
已开通的功能和行业方案
客户真实素材、案例、产品信息
当前获客目标
已经做过的内容和效果
```

### AI 可以做什么

```text
生成选题候选
生成标题和脚本初稿
把一个内容改成不同渠道版本
生成 CTA、私信和跟进话术
根据真实数据生成复盘建议
提醒客户缺少哪些关键资料
```

### AI 绝不能做什么

```text
编造案例、客户评价、数据、成交效果
替客户承诺无法验证的结果
在没有真实资料时伪造行业知识
自动向外发布内容或联系客户
```

## 系统 F：安全、授权、更新与服务

这是能不能卖给真实客户的底线，不是以后再说的小功能。

### 最低要求

```text
Windows 安装包签名
方案包签名
只信任你发布的方案包
客户授权码或账户授权
本地数据加密或至少受操作系统用户保护
本机备份、导出、恢复
软件版本更新
方案包版本更新
错误日志和隐私说明
```

### 授权要回答的问题

```text
这个客户买了哪些方案？
能装在几台设备？
是否到期？
是否允许升级？
客户换电脑后如何迁移？
方案停用后，旧数据是否可读？
```

## 三、完整产品的三个角色

| 角色 | 使用什么 | 最关心什么 |
|---|---|---|
| 终端客户 | 客户工作台、功能中心 | 做什么、怎么做、结果如何 |
| 你或部署人员 | 部署模式、管理台 | 给谁装什么、怎么配置、怎么续费 |
| 内容与行业制作人员 | 方案制作工具 | 把行业经验做成可销售方案 |

不要试图用同一个界面同时服务这三类人。

## 四、从完整蓝图倒推：什么必须先做

### P0：现在必须确认的产品规则

```text
Core 只做宿主，不默认启用获客功能。
所有客户功能都由方案包安装。
功能目录可预览，不等于已经开通。
你是唯一方案发布者。
客户工作台只展示已开通功能。
行业方案能补内容、字段、规则，也能增加专属功能。
```

### P1：第一版可卖产品必须有

```text
Windows Core
功能中心与完整预览
一个部署人员模式
私有方案包格式
方案包安装、依赖检查和回滚
本地客户数据存储与备份
2 个通用方案
1 个行业方案
方案版本与授权记录
```

### P2：有第一批付费客户后再做

```text
在线客户管理台
在线方案目录与更新服务
授权码 / 账户登录 / 设备管理
自动更新
更多行业方案
AI 正式接入
远程诊断与客户支持
```

### P3：稳定后再做

```text
可视化方案制作器
行业内容协作与审核
订阅、续费、加购
客户自助申请和支付
团队多人协作
第三方渠道数据接口
```

## 五、第一版的正确验收标准

第一版不是“能展示插件”，而是必须能完成这条真实交付链：

```text
你带着 Windows 安装包到客户现场
-> 安装 Core
-> 创建客户业务
-> 给客户预览方案
-> 选择短视频私信获客或卫浴到店预约方案
-> 导入并验证你发布的方案包
-> 自动生成对应工作台
-> 客户填写自己的真实资料
-> 产出第一条内容和一个明确 CTA
-> 记录第一条咨询来源
-> 下周能回看结果
```

只要这条链完整跑通，才算一个可以卖的“获客工作台”。

## 六、当前项目与完整蓝图的差距

当前已经具备：

```text
Windows 桌面外壳
空白工作台
功能中心
功能预览
动态菜单原型
部分通用功能原型
本机数据保存
```

当前还没有：

```text
真实方案包文件格式
方案包导入与签名验证
授权与客户设备管理
版本升级、迁移与回滚
部署人员模式与客户版隔离
行业内容包
完整选题库与咨询承接
在线管理台
AI 与真实知识库连接
备份、恢复、导出和隐私机制
```

因此，下一步不能直接堆行业页面；应先定义“方案包标准”和“部署链路”。
