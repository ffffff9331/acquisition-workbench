# 高质量成稿与持续学习闭环 v1

更新日期：2026-08-26

## 1. 目标

内容生产不能停在“生成一份草稿”和“给草稿打一个分数”。完整闭环应当回答四个问题：

1. 当前草稿具体哪里需要改？
2. 修改后是否仍然忠于真实来源、素材和经营目标？
3. 实际发布后带来了什么业务结果？
4. 哪些经过人工确认的经验可以进入下一次生成？

因此 Core 的内容链路扩展为：

```text
真实来源
→ 内容简报
→ 渠道草稿
→ 本地硬门槛
→ AI 深度评审
→ 候选优化稿
→ 人工采用
→ 重新评审
→ 锁定最终版本
→ 关联具体渠道任务
→ 手工发布与结果回流
→ 学习建议
→ 人工采用为后续规则
```

## 2. 候选优化稿

AI 不能直接覆盖当前草稿。生成候选优化稿必须满足：

- 读取当前内容简报、渠道规则、真实来源、当前草稿和最新评审。
- 优先保留评审中的“值得保留”部分。
- 逐项处理评审问题，不进行无目标的整篇重写。
- 不引入输入之外的价格、数据、案例、资质、标准、热度和效果承诺。
- 无法由现有来源解决的问题进入“仍需人工补充”，不得自行编造答案。
- 输出修改摘要、具体改动、未解决项和完整候选稿。

用户可以查看候选稿，但只有点击“应用候选优化稿”后才会替换当前草稿。应用前自动保存旧版本；应用后旧评审自动失效，必须重新评审和人工确认。

## 3. 持续学习对象

持续学习不是训练一个不透明的本地模型，而是保存一组可查看、可停止使用、可追溯的经营规则：

```text
继续保留
下次调整
明确避免
下次生成规则
解释边界
人工补充
```

每条学习记录保留：

```text
taskId
variantId
opportunityId
channelId
executionItemId
人工复盘结论
最终稿指纹
结果指纹
规则内容
服务来源
采用状态
建立时间
采用时间
```

## 4. 真实归因门槛

只有同时满足以下条件，才允许生成学习建议：

1. 当前渠道版本已经锁定。
2. 最终稿已经关联到具体渠道任务 `executionItemId`。
3. 该渠道任务已经记录发布或执行结果。
4. 当前获客机会已经由人工选择“继续投入、调整后再试或停止投入”。

系统不能只因为“同一个机会、同一个渠道”就假设某份稿件带来了某组结果。结果必须匹配具体渠道任务 ID。

## 5. 学习边界

AI 只能依据最终稿、人工复盘结论和真实结果整理建议：

- 单次结果不能外推为行业规律。
- 没有成交不能证明内容无效，也不能证明咨询质量稳定。
- 高曝光不能自动等同于高质量获客。
- 结果不足时，必须写明样本量、归因和观察周期限制。
- 人工复盘结论的优先级高于模型推测。
- AI 生成的学习建议不会自动进入后续生成。

只有用户点击“采用到后续生成”后，规则才进入可用规则池。用户可以随时停止使用或删除。

## 6. 后续生成如何使用规则

工作台只选择以下规则作为生成输入：

- 状态为“已采用”。
- 与当前渠道一致。
- 关联渠道任务、真实结果和人工复盘结论没有发生变化。

候选优化稿和下一次渠道草稿都会收到这些规则。提示词要求：

- 规则只作为本地经验和表达约束。
- 与当前简报或真实来源冲突时，以当前任务为准。
- 不得在对外内容中把本地规则描述成行业事实。

## 7. 当前不做

- 不自动训练或微调模型。
- 不上传客户全部历史数据建立云端画像。
- 不自动修改已采用规则。
- 不按 AI 分数自动决定继续投入或停止投入。
- 不把平台互动指标直接当成成交指标。
- 不在没有具体渠道任务归因时生成学习结论。

这套结构先保证每条经验可看、可解释、可撤销。真实客户积累足够样本后，再考虑规则聚合、冲突检测和多轮实验比较。
