# 积分充值与计费契约 v1

## 一、产品关系

```text
客户充值人民币
  -> 你的支付服务创建订单
  -> 微信支付 / 支付宝完成支付
  -> 支付平台回调你的服务端
  -> 服务端核验金额与订单状态
  -> 服务端增加工作台积分
  -> 客户发起一次生成
  -> 你的官方网关使用模型供应商 API Key
  -> 生成成功后服务端扣除积分并记录流水
```

客户充值的是“工作台积分”，不是 API Key。客户永远不应看到或获得模型供应商 API Key、支付商户密钥、内部模型名称、成本价和路由规则。

客户接入自己的第三方 API 时，模型费用由客户直接向第三方服务商支付，不经过工作台积分账户。

## 二、客户端已经实现

- 顶栏积分账户入口。
- 真实可用积分、冻结积分和更新时间。
- 由服务端返回的动态充值套餐。
- 服务端创建充值订单并返回收银台地址。
- 支付完成后的手工刷新余额。
- 充值、使用、退款、调整和赠送流水。
- 账户暂停、关闭、余额提醒、网络失败和网关未配置状态。
- 生成接口返回本次扣除积分与扣后余额。
- 充值订单和生成请求都携带客户端幂等号。
- 「形成获客机会」已接入统一生成通道：官方模式走积分网关，自有 API 模式不走积分。
- 账户、套餐、流水和扣费结果采用严格结构校验；非法或未知状态不会被客户端改造成“0 积分正常账户”。
- 流水明确区分处理中、已完成、失败和已退款。

## 三、服务端必须实现

### 读取积分账户

```http
GET /v1/workspaces/{workspaceId}/credit-account
Authorization: Bearer {工作台服务授权码}
```

返回：

```json
{
  "account": {
    "workspaceId": "client-workspace-id",
    "balance": 1280,
    "frozen": 0,
    "lowBalanceThreshold": 200,
    "status": "active",
    "updatedAt": "2026-08-26T18:30:00+08:00"
  },
  "packages": [],
  "transactions": []
}
```

积分使用整数；人民币金额使用“分”作为整数单位，禁止使用浮点数记账。

### 创建充值订单

```http
POST /v1/workspaces/{workspaceId}/recharge-orders
Authorization: Bearer {工作台服务授权码}
Content-Type: application/json

{
  "packageId": "standard",
  "clientRequestId": "UUID"
}
```

返回：

```json
{
  "orderId": "order-id",
  "checkoutUrl": "https://pay.example.com/order-id",
  "expiresAt": "2026-08-26T19:00:00+08:00"
}
```

服务端必须以 `clientRequestId` 做幂等控制。重复请求不能生成多个有效订单或重复入账。

### 官方生成

```http
POST /v1/generations
Authorization: Bearer {工作台服务授权码}
Content-Type: application/json

{
  "workspaceId": "client-workspace-id",
  "requestId": "UUID",
  "task": "acquisition_opportunities",
  "payload": {
    "brief": {
      "offer": "旧卫生间局部改造",
      "targetCustomer": "准备翻新老房卫生间的本地业主",
      "conversionGoal": "私信户型，预约初步判断"
    },
    "query": "旧卫生间翻新避坑",
    "evidence": []
  }
}
```

成功返回：

```json
{
  "requestId": "UUID",
  "output": {
    "topics": [
      {
        "title": "",
        "customerQuestion": "",
        "targetCustomer": "",
        "buyerStage": "方案比较",
        "demandSignal": "",
        "contentAngle": "",
        "keyPromise": "",
        "proofNeeded": "",
        "callToAction": "",
        "leadMagnet": "",
        "recommendedChannels": ["douyin"],
        "riskNote": "",
        "fitReason": "",
        "evidenceIds": []
      }
    ]
  },
  "usage": {
    "pointsCharged": 12,
    "balanceAfter": 1268
  }
}
```

同一个 `requestId` 重试时必须返回同一结果或同一处理状态，不能重复调用模型、重复扣除积分。

## 四、不可变规则

- 客户端不能自行增加、扣除、冻结或退回积分。
- 充值套餐、价格、赠送积分和兑换比例由服务端返回，不写死在安装包。
- 支付页面显示成功不等于到账，只有可信支付回调核验通过后才能入账。
- 支付回调必须验签、核对商户订单号、金额、币种和订单状态，并保证一次性入账。
- 模型调用失败、超时或结构校验失败时如何收费必须由服务端统一执行，客户端不能猜测。
- API Key、支付私钥、回调密钥和内部成本信息不得进入客户端、日志、错误信息或导出文件。
- 账务流水需要支持对账，不允许通过删除记录修正余额；应追加冲正或调整流水。
- 正式环境只允许 HTTPS 网关和 HTTPS 收银台地址。

## 五、当前诚实边界

客户端和接口契约已经完成，真实微信支付 / 支付宝支付、服务端账本、支付回调、模型路由和生产网关仍需要部署。默认 `electron/official-service.json` 中的网关地址为空，因此客户安装后不会看到模拟余额或模拟充值套餐。

客户端契约测试只验证请求格式、严格响应校验、幂等请求编号、生成扣分返回值和自有 API 绕过积分。它不能替代服务端的支付回调验签、重复回调幂等、并发扣分、失败生成不扣分和账务对账测试；这些必须在官方网关仓库和预发布环境中另行完成。
