# AI 服务与接入规范 v1

本文定义「获客工作台」的 AI 服务边界。它不是行业插件，也不是一个空白聊天框，而是工作台中所有获客能力可共用的生成基础设施。

## 一、面向客户的两种选择

客户在 Windows 工作台顶栏的「AI 服务」中只能看到两种清晰选择。

```text
官方 AI 服务（推荐）
接入自己的 AI 服务
```

### 官方 AI 服务

客户填写：

```text
工作台编号
服务授权码
```

客户可见的业务规则：

```text
使用预付余额
服务由工作台提供
模型、行业内容结构和输出质量由工作台持续维护
```

客户不应看到：

```text
模型供应商 Key
内部模型名称
提示词版本
网关地址
插件 manifest
成本价或路由规则
```

### 接入自己的 AI 服务

客户填写：

```text
OpenAI 兼容服务地址
模型名称
API Key
```

客户可见的业务规则：

```text
模型费用由客户直接向第三方服务商支付。
工作台提供行业流程、内容结构和基础连通性测试。
第三方模型的质量、速度、限流、价格和可用性由对应服务商决定。
```

这个规则必须出现在配置界面中，不能只写在合同或帮助文档里。

## 二、凭据保存规则

### 客户端绝不保存你的模型供应商 Key

禁止的结构：

```text
客户 Windows 工作台
-> 内置你的模型 API Key
-> 模型供应商
```

正确结构：

```text
客户 Windows 工作台
-> 工作台编号 + 服务授权码
-> 你的 AI 服务网关
-> 你的模型供应商
```

官方服务授权码和自有 API Key 都属于秘密凭据。Windows 桌面版通过 Electron `safeStorage` 保存；Windows 上该能力使用系统 DPAPI。秘密凭据不得出现在：

```text
localStorage
客户业务数据 JSON
导出文件
控制台日志
错误弹窗
复盘数据
同步备份
```

客户端只能读取“凭据是否已保存”的状态，不能从主进程把原始 Key 再取回界面。

## 三、官方 AI 服务网关职责

网关是后续预付费服务的唯一计费点。一次生成请求应经历：

```text
1. 验证工作台编号和服务授权码
2. 判断客户是否已开通对应获客能力
3. 检查可用余额或套餐额度
4. 读取客户的行业打法包与业务资料
5. 组合任务、规则和结构化输出要求
6. 调用模型供应商
7. 扣减实际消耗并记录账务流水
8. 返回结构化结果
```

建议的客户端请求形态：

```json
{
  "task": "short_video_script",
  "workspaceId": "client-workspace-id",
  "businessContext": {
    "industry": "bathroom",
    "businessName": "示例门店"
  },
  "playbookId": "bathroom-short-video-v1",
  "input": {
    "topic": "小户型卫生间最容易买错的 3 个东西"
  }
}
```

授权码应在 HTTP Authorization Header 中传递，而不应写入请求体或日志。生产环境使用 HTTPS。

网关应返回稳定的结构化内容，而非一段未经整理的聊天文本。例如短视频任务至少返回：

```text
选题
目标客户
开头
分镜 / 口播段落
拍摄清单
标题备选
发布文案
行动引导
```

## 四、第三方服务兼容边界

第一版支持 OpenAI 兼容接口。客户端的「测试连接」使用：

```text
GET {服务地址}/models
Authorization: Bearer {API Key}
```

测试通过只证明：

```text
该地址可访问
该凭据具备基础权限
服务返回了正常状态
```

测试通过不代表：

```text
模型擅长中文
模型能遵循行业打法包
内容符合平台规范
输出质量与官方服务相同
服务商不会限流或变更价格
```

以后调用第三方生成接口时，客户端仍应强制传入工作台的任务结构、打法包和 JSON 输出约束。这样可以维持工作流一致性，但不能承诺结果质量。

## 五、插件、打法包与 AI 服务的关系

```text
获客工作台 Core
+ 获客能力插件
+ 行业打法包
+ AI 服务
= 客户专属工作台
```

其中：

```text
短视频 / 小红书 / 社区 / 视频号 / 转介绍 / 活动
= 获客能力插件

卫浴 / 门窗 / 阳台柜等行业选题、话术、标准和复盘规则
= 行业打法包

官方服务或客户自有 API
= AI 服务
```

插件只声明“需要什么生成任务”，例如：

```text
短视频能力：选题、脚本、标题、发布文案、私信承接
社区能力：活动主题、物料文案、登记话术、后续跟进任务
转介绍能力：回访邀请、晒单文案、奖励说明、转介绍跟进
```

插件不自己保存 API Key，也不自己决定模型供应商。

## 六、当前客户端已实现与待实现项

已经实现：

```text
全局 AI 服务入口
官方服务授权配置
自有 API 配置
Windows 系统加密保存凭据
隐藏已保存凭据
自有 API 基础连通性测试
自有 API 的聊天补全兼容层与 JSON 校验
客户界面的质量与责任边界提示
官方积分账户客户端
真实余额、冻结积分和用量流水读取
动态充值套餐和充值订单接口
生成请求的幂等号、扣除积分和扣后余额契约
基于业务目标和真实来源证据的获客机会生成入口
获客任务、至少两条来源和人工采用门槛
目标客户、购买阶段、需求信号、证明素材、承接动作和风险提示的结构化输出
获客机会向抖音、小红书、微信、B站和线下活动执行区的人工交接
机会与渠道选题 / 任务 ID 的持久关联
发布、咨询、登记线索、有效线索和成交结果回传
“继续投入、调整后再试、停止投入”的人工复盘结论
下一次机会分析接收已复盘的本地真实实验结果
官方生成成功后刷新扣后余额
自有 API 生成绕过积分账户
```

下一阶段实现：

```text
官方网关服务端与工作台授权校验
微信支付 / 支付宝支付和服务端回调入账
官方网关中的真实模型调用和账务执行
采用机会后的短视频脚本、笔记正文和触达话术生成
服务端对历史结果输入的脱敏、数量限制和跨工作台隔离
行业打法包的读取和版本控制
微信支付 / 支付宝支付回调与生产账务验收
```

在官方网关尚未部署前，客户端不得声称“官方服务已经可以生成内容”，也不得展示虚假的余额、充值套餐或账务流水。当前客户端只在网关真实返回账户数据后展示余额和充值入口。
