# 通用获客模板 Base Template v1（定稿）

> 本文是当前唯一的通用模板定稿。它不包含行业、客户插件、商业化、技术方案或具体渠道打法。
> 共识来源：Codex 与 Claude 于 2026-08-24 的产品讨论。

## 一句话定义

```text
一张把“获客目标”变成“可追踪内容行动”，再变成“下周决策”的项目模板。
```

它不是内容生成器。内容生成只是模板中间的一个步骤。

## 一、严格收缩后的设计原则

1. 先设定目标，后生产内容；不允许反过来。
2. 人群、问题和解决方案不拆开填，合为一个“目标场景”。
3. 证据不是每个项目的必填环节，而是内容生产时可选的支撑素材。
4. CTA 和追踪码是两件不同的事：CTA 让人行动，追踪码让系统知道人从哪里来。
5. 复盘不做主观总结，只基于内容、线索和有效线索数据做继续/优化/停止决定。

## 二、四个核心对象

| 对象 | 是什么 | 最少必须记录什么 |
| --- | --- | --- |
| 客户 | 内容和项目的归属容器 | 名称、业务描述 |
| 获客项目 | 一次完整获客行动，从目标到复盘 | 目标、目标场景、内容方向、周期 |
| 内容项 | 一条可以独立发布、独立评估的内容 | 选题、内容结构、制作要求、渠道版本、追踪码 |
| 线索记录 | 由某条内容带来的潜在客户信息 | 来源内容、线索信息、质量标记 |

说明：渠道版本、CTA、追踪码、发布状态属于“内容项”；复盘属于“获客项目”的结果，不另做复杂对象。

## 三、五个固定页面

### 1. 客户列表

**目的：** 管理和切换不同客户的上下文。

```text
必要字段：客户名称、业务描述
可选字段：地址、联系方式、负责人/品牌人设
必要操作：新建、切换、复制客户
```

### 2. 项目设定

**目的：** 先把这次获客行动讲清楚。

```text
目标：希望谁完成什么关键行为、数量、截止时间
目标场景：目标人群是谁 + 正在经历什么问题 + 我提供什么解决方案
内容方向：这次项目主要准备做哪类内容
```

完成后，用户选择 1-3 个具体选题，进入内容流水线。

### 3. 内容流水线

**目的：** 从选题产出可制作、可发布、可追踪的内容。

**内容生产：**

```text
选题：这条内容解决什么问题/传递什么信息
内容结构：开头钩子 -> 主体（观点 + 可选证据） -> 结尾 CTA
制作要求：拍摄指令、图文要求或素材清单
证据素材（可选）：案例、数据、产品图、过程、资质等
```

**渠道适配：**

```text
选择渠道
-> 生成每个渠道的标题、正文/脚本、发布文案
-> 为每个渠道版本生成唯一追踪码
```

**执行状态：**

```text
待制作 -> 待发布 -> 已发布 -> 已复盘
```

### 4. 线索追踪

**目的：** 把咨询明确回连到内容，而不是做完整 CRM。

```text
来源内容：来自哪条内容、哪个渠道版本
线索信息：联系方式或需求描述、来源时间
质量标记：高 / 中 / 低
状态：待跟进 / 已转化 / 已放弃
备注：补充信息
```

来源可由追踪码自动识别；识别不了时允许人工选择，但不可留空。

### 5. 项目复盘

**目的：** 用数据给出下周决策。

```text
内容效果排行：播放/阅读（可手动录入）、线索数、有效线索数
内容方向对比：不同内容方向的转化表现
渠道对比：不同渠道的线索和有效线索表现
下周决定：继续 / 优化 / 停止，并附数据理由
```

## 四、一条固定流程

```text
1. 新建客户
2. 设定项目：目标 + 目标场景 + 内容方向
3. 生产内容：选题 -> 内容结构 -> 制作要求 -> 渠道版本 -> CTA + 追踪码
4. 发布内容：标记为已发布
5. 记录线索：关联来源内容并标记质量
6. 复盘项目：比较数据，决定继续/优化/停止
```

这条流程是 Base Template 的骨架。任何后续扩展只能让每一步更贴合，不能跳过、替换或颠倒它。

## 五、五条不可变约束

### 1. 目标必须明确且可量化

每个项目必须写清“谁要完成什么关键行为、数量、截止时间”。

不接受“提升品牌知名度”这种无法观察的目标。

### 2. 每条内容必须有 CTA 和追踪码

CTA 是用户下一步要做什么；追踪码是用来记录来源的唯一标识。两者缺一不可。

不允许只生成内容文案、不设置行动与追踪。

### 3. 每条线索必须关联到具体内容

线索必须说明它来自哪条内容、哪个渠道版本。没有来源的线索可以进入待补充列表，但不能计入复盘结论。

### 4. 复盘必须基于数据

复盘必须展示内容与线索的关系，并给出继续/优化/停止。不能由 AI 没有数据依据地推荐。

### 5. 流程顺序不可跳过或颠倒

```text
目标 -> 内容 -> 发布 -> 线索 -> 复盘
```

没有目标，内容是散发；没有线索记录，复盘是空谈。

## 六、这一版故意不包含的内容

```text
行业字段、客户专属规则、选题库、合规词库、具体平台策略、
视频剪辑、图片设计、自动发布、自动私信、CRM、广告投放、插件机制。
```

这些不是 Base Template 本身。只有这张通用模板跑顺后，才允许作为“在固定插槽里补充的内容”进入下一阶段。
